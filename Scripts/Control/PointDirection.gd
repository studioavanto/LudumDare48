extends Node2D


var ideal_look_at_dir = Vector2(0.0, 0.0)
onready var player_char = get_parent().get_node("PlayerBody")

func _ready():
	pass # Replace with function body.

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
